'use client'

// Theme toggle is disabled — website is locked to light mode.
// The button is hidden so users can't switch to dark mode.
// To re-enable: remove `forcedTheme="light"` from layout.tsx and
// uncomment the button below.

export function ThemeToggle() {
  return null
}
